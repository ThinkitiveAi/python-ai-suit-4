volumes:
  customehr_local_postgres_data: {}
  customehr_local_postgres_data_backups: {}
  customehr_local_redis_data: {}

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: customehr_local_django
    container_name: customehr_local_django
    depends_on:
      - postgres
      - redis
      - mailpit
    volumes:
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: customehr_production_postgres
    container_name: customehr_local_postgres
    volumes:
      - customehr_local_postgres_data:/var/lib/postgresql/data
      - customehr_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  mailpit:
    image: docker.io/axllent/mailpit:latest
    container_name: customehr_local_mailpit
    ports:
      - "8025:8025"

  redis:
    image: docker.io/redis:6
    container_name: customehr_local_redis
    volumes:
      - customehr_local_redis_data:/data
